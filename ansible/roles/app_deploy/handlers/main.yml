---
- name: Restart sample-app
  command: kubectl rollout restart deployment/sample-app -n {{ k8s_namespace }}
  listen: "restart sample-app"

- name: Restart agent-backend
  command: kubectl rollout restart deployment/agent-backend -n {{ k8s_namespace }}
  listen: "restart agent-backend"
